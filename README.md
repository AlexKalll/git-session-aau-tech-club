# git-session-aau-tech-club
This is demo repository for version control system (git n' github) session at AAU Tech Club Bootcamp.

a = 1
this is html file

Connection:
- `index.html` loads `scripts.js` and contains a `div#project-info` with `data-name` and `data-version`.
- `scripts.js` reads those attributes and displays the project name and version.
- `setup.py` stores the same metadata in the `METADATA` dictionary and exposes `get_metadata()`.
